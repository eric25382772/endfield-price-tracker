# Endfield Price Tracker 封裝建議

## 目前方式（一鍵安裝包）

目前流程：

``` text
Setup.exe
│
├─ 安裝 Python
├─ pip install requirements
├─ 安裝 Playwright
├─ 下載 OCR 模型
└─ 建立桌面捷徑
```

### 優點

-   使用者只要執行 Setup 即可。
-   容易更新 Python 程式。
-   開發成本較低。

### 缺點

-   第一次安裝時間較長。
-   需要安裝 Python 與套件。
-   容易受到使用者電腦環境影響。

------------------------------------------------------------------------

## EXE 封裝

建議使用：

-   PyInstaller
-   Nuitka（較推薦，效能與相容性通常較佳）

封裝後：

``` text
EndfieldTracker.exe
```

EXE 內包含：

-   Python Runtime
-   所有 Python 套件
-   專案程式

使用者不需要另外安裝 Python。

### 優點

``` text
下載 EXE
↓
雙擊
↓
程式啟動
```

-   使用者體驗最好。
-   不依賴系統 Python。
-   安裝流程最簡單。

### 缺點

-   EXE 體積較大（約數十到數百 MB）。
-   每次重新封裝需要重新建置。

------------------------------------------------------------------------

## OCR 模型建議

不要直接打包進 EXE。

建議流程：

``` text
第一次啟動
↓
檢查 OCR 模型
↓
不存在
↓
自動下載
↓
下載完成
↓
之後直接使用
```

優點：

-   EXE 不會過大。
-   更新模型較容易。
-   不需要重新發布整個 EXE。

------------------------------------------------------------------------

## Docker 是否適合？

目前判斷：

**不建議。**

原因：

-   程式需要操作 Windows。
-   需要擷取遊戲畫面。
-   需要全域快捷鍵。
-   需要 Win32 API。
-   Docker 反而增加安裝與使用門檻。

因此 Docker 對此專案沒有明顯優勢。

------------------------------------------------------------------------

# 建議最終架構

``` text
EndfieldTracker.exe
│
├─ 首次啟動
│   ├─ 檢查 OCR 模型
│   ├─ 不存在則自動下載
│   └─ 建立設定資料夾
│
├─ 之後直接執行
│
└─ 自動檢查更新（可選）
```

------------------------------------------------------------------------

# 希望 Claude Code 協助完成

1.  使用 Nuitka（或 PyInstaller）封裝成 Windows EXE。
2.  使用者不需安裝 Python。
3.  第一次啟動自動下載 OCR 模型到 `%LOCALAPPDATA%`。
4.  保留目前所有功能與 GUI。
5.  保留設定檔與自動更新機制（若已有）。
6.  盡量縮短首次啟動與後續啟動時間。
7.  維持對一般使用者「下載 → 雙擊 → 使用」的體驗。
